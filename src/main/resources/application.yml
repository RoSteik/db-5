spring:
  datasource:
    url: jdbc:mysql://34.118.6.159:3306/rostyks
    username: rostyk
    password: rostyk
  jpa:
    hibernate:
      # Hibernate ddl auto (validate, create, create-drop, update)
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        default_schema: public

#spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/rostyks?enabledTLSProtocols=TLSv1.2
#    username: root
#    password: rpv22122003
#    driver-class-name: com.mysql.cj.jdbc.Driver
#  jpa:
#    # Show or not log for each sql query
#    show-sql: true
#    hibernate:
#      # Hibernate ddl auto (validate, create, create-drop, update)
#      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
#
#  mvc:  # the fix for swagger
#    pathmatch:
#      matching-strategy: ant_path_matcher
#
#server:
#  port: 8080

